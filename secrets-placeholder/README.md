This folder is a "template" of sorts for the `secrets` folder (The `secrets` folder is a private git submodule).

After cloning, copy the contents of this folder into the `secrets` folder and fill in the blanks.
